# RAG Site Chat — Python 依赖
# 安装: pip install -r requirements.txt

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# HTTP 客户端（LLM API 调用）
httpx>=0.27.0

# RAG 引擎（embedding 通过 OpenRouter API，无需本地模型下载）
chromadb>=0.5.0

# HTML 解析（日报/网页数据源）
beautifulsoup4>=4.12.0

# 环境变量
python-dotenv>=1.0.0
